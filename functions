# mkdir && cd
mkcd() {
    mkdir -p "$@" && cd "$_"
}

# Show Git branch name
parse_git_branch() {
    git branch 2> /dev/null | sed -n -e 's/^\* \(.*\)/(\1)/p'
}

# Enable prompt substitution (only for zsh)
if [[ -n "$ZSH_VERSION" ]]; then
    setopt PROMPT_SUBST
fi